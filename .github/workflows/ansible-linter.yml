name: Ansible Linter Workflow

on:
  push:
    branches-ignore: [main]    # Runs on pushes to ANY branch except main
  pull_request:
    branches: [main]           # Runs on PRs targeting main
  workflow_dispatch:           # Allows manual triggering

jobs:
  ci:
    name: Code Quality
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          
      - name: Install tools
        run: |
          python -m pip install --upgrade pip
          pip install ansible-lint molecule

      - name: Run fixes for Ansible Lint
        run: ansible-lint --fix  # Automatically fix linting issues

      - name: Ansible Lint
        run: ansible-lint  # Code quality check
      
      - name: Molecule Test
        run: molecule test  # Run Molecule tests